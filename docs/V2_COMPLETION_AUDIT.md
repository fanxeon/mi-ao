<!-- Copyright (c) 2026 FanXeon@Poemcoder with Codex -->

# V2 交付收口审计

> 现场核对：2026-07-16 · 版本：`0.2.0` · 阶段：source-first beta / V2

本文档将“代码已实现”、“自动门禁已通过”、“安装产物已验证”和“当前 Mac 已完成系统授权”分开记录。终端子进程的辅助功能或蓝牙状态，不代替从 Finder / LaunchServices 启动的米遥 App 状态。

## 逐项证据

| 收口项 | 真实实现 | 自动或产物证据 | 当前结果 |
| --- | --- | --- | --- |
| 内部环境隔离 | `ProcessEnvironment.swift` 与 `scripts/lib/environment.sh` 在所有外部进程边界删除任意 `MI_AO_*` | Swift 与 `EnvironmentIsolationTests.sh` 使用未知未来变量回归 | ✅ 完成 |
| BLE 设备选择 | 扫描与已连接设备合并，UUID 持久化，固定设备优先，能力/信号/UUID 确定性仲裁 | 设备目录、优先级和多设备单测 | ✅ 完成 |
| 断连恢复 | 普通断连不终止长运行会话，按 `1 → 2 → 4 → 8 → 16` 秒分级退避，显示尝试次数与下次延迟 | 退避单测；安装版对不存在 UUID 的真实反馈已验证 | ✅ V2 基线完成；8 小时压测属 1.0 门禁 |
| 能力协商看门狗 | `GET_CAPS` 每 1.5 秒最多尝试 3 次；仍无响应则显示原因、主动断开并进入分级重连；重连先查已保存 UUID / 系统已连接 ATVV，再广播扫描；协商前忽略语音事件 | 纯策略单测；真机确认超时进入退避且正常响应仍在首次请求后就绪；停止广播时可从系统已连接设备恢复 | ✅ 修复永久“连接中”与重连漏设备 |
| HID 事件 | 数组 Usage 归一化，按下/重复/松开收敛为成对事件，异常结束释放修饰键 | HID reducer、HOME 单/双击、快捷键安全单测；映射 Shell 异常信号恢复测试 | ✅ 实现与门禁完成 |
| 按键配置 | 运行时热重载、真实 HID 高亮、单次测试、私有权限导入/导出、完整 catalog 校验 | 热重载、TV 目标、保留键、危险快捷键、1 MB 上限和导入回归 | ✅ 完成 |
| 单次测试反馈 | 未映射与语音键不可测；只在执行器返回真实动作/成功/失败后显示结果；HOME 保留到 350 ms 仲裁窗口结束 | Swift 构建与执行器现有单测 | ✅ 修复 fake success 与 HOME 早释放 |
| 设置窗口 | 原生分段控件、首次/日常语境分离、动态深浅色、380 px 最小宽、390 px 内容复核、可读的 AX 标签与真实状态卡 | 设置状态单测、App 产物真实页面检查 | ✅ 完成 |
| 运行中再次打开 | 日常运行经 LaunchServices 启动并进入真实 AppKit 事件循环；reopen 复用现有进程显示设置窗口 | delegate / 会话令牌 PID 单测、LaunchServices argv Shell 门禁、当前 Mac 真实重复打开 | ✅ 修复“米遥不能再打开” |
| 菜单栏状态 | 基础运行态 + 1.2 秒指令态；方向、滚动、Return/Escape、HOME、快捷键、模式/配置、Codex 操作使用对应图标 | 状态优先级、SF Symbol 存在性、真实成功/失败回传单测 | ✅ 完成 |
| 执行高亮底色 | 普通指令蓝色，可确认成功绿色，失败红色，使用圆角 `CALayer`；录音/转写/断连/错误优先 | 菜单栏呈现单测与打包后 SF Symbol 校验 | ✅ 完成 |
| 录音结束状态 | 过短录音取消后立即发布 ready；队列满显示真实错误并在 2 秒后回到实际 processing / ready；关闭中不回刷 ready | 语音真机发现并复现过期 recording；状态优先级与队列容量单测 | ✅ 修复菜单栏卡在录音态 |
| 原子安装 | 运行中拒绝覆盖，同卷 staging，替换前后签名验证，App 与安装上下文任一失败恢复 | `InstallAppTests.sh` 覆盖运行锁拒绝、首装和签名失败回滚 | ✅ 完成 |
| Whisper 模型完整性 | `Resources/WhisperModel.sha256` 是 Shell / App 共用单一契约；下载、设置页、转写器边界和安装验证全部校验 | 同尺寸篡改运行时拒绝、损坏下载不替换旧模型、安装版 148 MB 模型实际 hash 通过 | ✅ 完成 |
| 版本 / CI / 文档 | `VERSION`、Info.plist、Swift 6、README、进度文档和 CI `make check` 统一 | `ReleaseContractTests.sh` 防漂移 | ✅ 完成 |

## 2026-07-16 现场验收

- `make check` 全部通过：66 个 Swift Testing 用例、9 组 Shell 门禁、App Bundle 签名、plist 和 `git diff --check`。
- `/Users/fanx/Applications/米遥.app` 已原子更新为包含本批修复的 `0.2.0`；深度签名、Bundle ID、内置 Runtime、安装上下文与代码指纹通过。
- 安装版以 `--no-buttons --no-submit` 在当前已连接小米遥控器上枚举 GATT，协商 `ATVV v1.0 / ADPCM 16 kHz / 120 B`，进入“桥接已就绪”。
- 安全中断后无米遥残留进程、无 runtime lock、无米遥映射状态，`hidutil` 仍为原始空映射。
- 从 LaunchServices 打开的新 App 真实显示：Whisper 148 MB 模型 SHA-256 已验证、Codex 兼容已开启、内置启动组件可用。
- 最新 `dist/米遥.app` 经 LaunchServices 在真机上进入 `ATVV v1.0 / ADPCM 16 kHz / 120 B` 就绪；运行中再次打开正确显示“米遥设置”，不再出现“不能再打开”。安全 TERM 后 App 和等待它的 LaunchServices 进程均立即结束，映射保持原始空状态。
- 最终安装版全功能运行真实触发 `GET_CAPS` 三次超时、`1 → 2 → 4 → 8 → 16` 秒退避，并在每轮从 macOS“系统已连接 ATVV 设备”重新连接；未回能力包时始终没有伪报桥接就绪。
- 安装版运行中复制临时按键配置后，运行日志确认热更新到副本；删除后确认热更新回官方默认。安全退出后无 App / LaunchServices 残留、无 runtime lock，系统映射恢复为空。
- 最终安装包重新获得辅助功能授权后，经内置 `start.sh → run-with-mapping.sh → LaunchServices` 全功能路径启动；运行锁 PID 与真实 App PID 同为 `31543`，12 键中性映射有效，并在首次 `GET_CAPS` 后 30 ms 协商 `ATVV v1.0 / ADPCM 16 kHz / 120 B`。菜单栏真实回到“已就绪 · 按住语音键说话”。
- 真机依次按下方向上、确认、TV、语音，外部监听收到四组完整 `down/up`，没有漏键；TV 动作实际切换为“方向键（仅方向环发送上下左右）”。
- 真机短按语音键实际产生 `AUDIO_START / AUDIO_STOP`，240 samples 被判定为过短录音；取消后菜单栏从“正在听”恢复为“已就绪 · 按住语音键说话”，未保留过期状态。
- 最终全功能运行中再次打开 App 只带回“米遥设置”，没有第二运行实例或“米遥不能再打开”。真实 `dpad_up` 按下后的瞬时菜单采样为“移动鼠标 · 上”，证明物理 HID → 动作执行 → 对应菜单图标/圆角指令反馈链路已闭环。

## 当前 Mac 已完成的 V2 现场门禁

最终安装包完成替换并由用户重新添加辅助功能授权后，当前 App 真实设置页状态是：

- 米遥辅助功能：已允许控制 Codex 与执行遥控器动作。
- 蓝牙：已允许，设置页可读取真实授权状态。

系统授权、安装版热更新、退出恢复、全功能 BLE 就绪、实体方向/OK/TV/语音事件和菜单栏瞬时指令反馈均已完成当前小米 2 Pro 固件 2671 的现场验收。高亮底色的蓝/绿/红规则由呈现层测试覆盖；物理方向键已现场确认进入对应“移动鼠标 · 上”指令态。

## 不属于 V2 已完成声明

- Developer ID 签名、Apple 公证、DMG 和自动更新。
- 第二类遥控器的可复核真机证据。
- 注销/重新登录的登录项系统验收。
- 8 小时蓝牙干扰环境压测。

这四项是 1.0 发布门禁，不用单元测试或小米 2 Pro 的现有证据替代。
